<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:SportSpark.ViewModels"
             xmlns:views="clr-namespace:SportSpark.Views"
             x:Class="SportSpark.Views.HomeView"
             x:DataType="viewModels:HomeViewModel"
             Shell.NavBarIsVisible="False">
    <Grid x:Name="MainContainer" IgnoreSafeArea="{OnPlatform Android=False, iOS=True}" RowDefinitions="65, *"
              ColumnDefinitions="*, *" BackgroundColor="{StaticResource BackgroundLight}">

        <Grid BackgroundColor="{StaticResource BackgroundLight}" RowDefinitions="*" ColumnDefinitions="*, *" Grid.ColumnSpan="2">
            <Label Text="{Binding FirstName,StringFormat='Welcome, {0}'}" TextColor="Black" Grid.Column="0" 
                   VerticalOptions="CenterAndExpand" FontSize="20" HorizontalTextAlignment="Center" FontAttributes="Bold"
                   HorizontalOptions="Fill"/>
            <Border WidthRequest="45" HeightRequest="45" VerticalOptions="CenterAndExpand" HorizontalOptions="End" Margin="22, 0"
                    Stroke="{StaticResource SportSparkDarkBlue}" StrokeThickness="1" Grid.Column="1">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                </Border.GestureRecognizers>

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="28"/>
                </Border.StrokeShape>

                <Image  VerticalOptions="CenterAndExpand" HorizontalOptions="Center" WidthRequest="50" HeightRequest="50"
                                Source="anonymous_person.jpg">
                    <Image.Clip>
                        <EllipseGeometry Center="25,25" RadiusX="25" RadiusY="25"/>
                    </Image.Clip>
                </Image>
            </Border>
        </Grid>

        <ScrollView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HorizontalScrollBarVisibility="Never" HorizontalOptions="FillAndExpand"
                        VerticalScrollBarVisibility="Never" Grid.RowSpan="2">

            <Grid ColumnDefinitions="*, *" Grid.ColumnSpan="2" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout Spacing="12" Margin="15, 0" Grid.ColumnSpan="2">

                    <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" ColumnDefinitions="*, *" RowDefinitions="75, *" Margin="0, -10, 0, 0">
                        <Label Text="Near you" FontSize="16" TextColor="Black" VerticalOptions="CenterAndExpand" HorizontalOptions="StartAndExpand" Grid.Column="0"/>
                        <Label Text="See more" FontSize="14" TextColor="{StaticResource LightThemeEntry}" VerticalOptions="CenterAndExpand" Grid.Column="1" 
                                   HorizontalOptions="End"/>
                    </Grid>

                </VerticalStackLayout>
            </Grid>

        </ScrollView>
        <Grid Grid.Row="2" VerticalOptions="End" TranslationY="300" x:Name="btmGrid" Grid.ColumnSpan="2">
            <views:MenuView HeightRequest="300" Padding="10" Grid.RowSpan="2" Grid.ColumnSpan="2"/>
        </Grid>
    </Grid>
</ContentPage>